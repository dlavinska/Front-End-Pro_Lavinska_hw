const one = Number(prompt("Enter first number:", " "));

const two = Number(prompt("Enter second number:", " "));

const three = Number(prompt("Enter third number:", " "));

const result = (one + two + three) / 3;

alert(`Avarage of three numbers = ${result}`);